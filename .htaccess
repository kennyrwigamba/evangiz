    # Evangiz Restaurant Rewrite Rules & Security Configuration

    # Make sure Apache serves index.php for folder requests.
    DirectoryIndex index.php

    # Disable directory browsing and MultiViews (can interfere with clean URLs).
    Options -Indexes -MultiViews

    # Enable rewrite engine.
    RewriteEngine On

    # Never rewrite the front controller itself.
    RewriteRule ^index\.php$ - [L]

    # Allow existing files and directories to pass through untouched.
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Route all other requests to the front controller.
    RewriteRule ^ index.php [QSA,L]

    # Block direct access to database files
    <FilesMatch "\.(sql)$">
        Order deny,allow
        Deny from all
    </FilesMatch>

    # Block direct access to config.php
    <Files config.php>
        Order deny,allow
        Deny from all
    </Files>
